# journal Roadmap

## Ingested 2026-07-25
- [x] Update journal skill so entries are a single word only — `~/.claude/skills/journal/SKILL.md` slug rule now requires a single-word slug, picking the topic that mattered most. Not retroactively applied to already-published post filenames/URLs (would break live links); applies going forward on the next `/journal` run.
